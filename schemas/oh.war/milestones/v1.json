{"$defs":{"ExecutorKind":{"description":"Who executes a stage (SAS §23.4).","enum":["human","agent","service","katana","blut","laboratory"],"type":"string"},"Milestone":{"description":"An acceptance checkpoint (SAS §23.1). Carries no executor.","properties":{"depends_on":{"items":{"type":"string"},"type":"array"},"id":{"type":"string"},"obligation_refs":{"items":{"type":"string"},"type":"array"},"stage_refs":{"items":{"type":"string"},"type":"array"},"title":{"type":["string","null"]}},"required":["id"],"type":"object"},"Port":{"description":"A named typed port on a stage (SAS §23.5).","properties":{"name":{"type":"string"},"type_name":{"type":"string"}},"required":["name","type_name"],"type":"object"},"ResponsibilityTier":{"description":"Executor responsibility tier (SAS §26).\n\n§26.5: tier and executor kind are ORTHOGONAL. A T1 stage may be executed by\nany kind of actor, and nothing here may infer one from the other.","oneOf":[{"const":"T1","description":"Constitutional or mission-critical (§26.1).","type":"string"},{"const":"T2","description":"Implementation judgment (§26.2).","type":"string"},{"const":"T3","description":"Minor implementation detail (§26.3).","type":"string"},{"const":"T4","description":"Mechanical (§26.4).","type":"string"}]},"Stage":{"description":"A dispatchable execution node (SAS §23.3). Carries no obligations.","properties":{"budget_tokens":{"description":"The token budget for this stage's Dispatch (slice C2); absent means\nthe repository default applies.","format":"uint64","minimum":0,"type":["integer","null"]},"context_artifacts":{"description":"Repository paths the stage reads as inputs.","items":{"type":"string"},"type":"array"},"context_atoms":{"description":"Whole atoms this stage needs beyond the required ones, by file name.","items":{"type":"string"},"type":"array"},"context_external":{"description":"External references, recorded and never fetched.","items":{"type":"string"},"type":"array"},"context_sections":{"description":"Sections of atoms, as `<atom file>#<heading>`.","items":{"type":"string"},"type":"array"},"executor_args":{"description":"Arguments the executor's stage takes, as a JSON object.\n\nWritten as a JSON string scalar rather than nested YAML because the atom\nreader is a restricted subset (OW-ADR-0003) that refuses a mapping under\na sequence item — deliberately, so an atom cannot grow arbitrary YAML.\nA scalar carrying JSON keeps the args expressible without widening the\ngrammar, and it is the same shape BLUT's own `--args` takes.\n\n`None` lowers to `{}`, which is correct for a stage whose arguments are\nall optional and wrong for one whose are not — the executor decides, and\nsays so. This does not try to know which.\n\nHeld as the RAW scalar. Parsing it needs `serde_json`, and this crate's\nproduction dependencies are deliberately serde, thiserror and uuid; the\nseam that lowers a stage already has a JSON parser, so the string\ncrosses the boundary and is parsed there rather than widening core to\nhold a `Value`.","type":["string","null"]},"executor_kind":{"$ref":"#/$defs/ExecutorKind"},"executor_ref":{"description":"The name this stage's EXECUTOR knows it by (§49.2).\n\nA WAR stage id (`STAGE-002`) is an identifier inside this Warrant. It is\nnot what BLUT, Katana or a laboratory calls the thing that runs. The\nadapter used the id as the BLUT stage name, which meant every lowering\nnamed `STAGE-NNN` and was refused for naming a stage no cookbook has —\na refusal that looked like the pinned-registry rule working, when in fact\nthe question \"which executor stage is this?\" had never been asked.\n\nDeliberately executor-NEUTRAL. `katana` and `laboratory` stages have the\nsame problem and get the same field, rather than each seam growing a\nprivate one.\n\n`None` is honest for a stage nobody has bound yet; the adapter refuses to\nlower it rather than guessing.","type":["string","null"]},"id":{"type":"string"},"inputs":{"items":{"$ref":"#/$defs/Port"},"type":"array"},"outputs":{"items":{"$ref":"#/$defs/Port"},"type":"array"},"responsibility_tier":{"$ref":"#/$defs/ResponsibilityTier"},"title":{"type":["string","null"]},"wall_time_seconds":{"description":"Wall-clock bound for a `service` stage run by `war run` (slice C4b);\nabsent means the repository default applies.","format":"uint64","minimum":0,"type":["integer","null"]}},"required":["id","executor_kind","responsibility_tier"],"type":"object"}},"$id":"oh.war/milestones/v1","$schema":"https://json-schema.org/draft/2020-12/schema","description":"A validated milestone graph.","properties":{"milestones":{"items":{"$ref":"#/$defs/Milestone"},"type":"array"},"stages":{"items":{"$ref":"#/$defs/Stage"},"type":"array"}},"required":["milestones","stages"],"title":"MilestoneGraph","type":"object"}
