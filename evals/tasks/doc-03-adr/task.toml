schema = "oh.war/eval-task/v1"
sentence = "record why async is refused outside the MCP transport"
kind = "document"
expected_gate = "document.review@1.0.0"
max_tokens = 12000
max_wall_secs = 120
expected = "would_satisfy"
