# Legacy gaps: review queue, not reconciliation acceptance

Read-only independent Spec review against source `df2701da` inspected the13
structurally ready but NOT SATISFIED records. Their original contracts, evidence
and signatures remain unchanged. No mutating plants or real provider runs were
performed for this review. The receipts are preserved in
[OW93 evidence](../warrants/OW-WAR-0093/implementation/evidence/legacy-closure.json).

| Warrant | Missing observation or implementation | Next bounded action |
| --- | --- | --- |
| 0006 | OBL-003 names historical commit3678455, unavailable in current history | Recover original provenance or explicitly reconcile missing history; current-dated Progress Log, never backdated authorization |
| 0011 | Core Preflight exists; promised CLI boundary absent | Expose all six dimensions and plant named unchecked dimension refusing readiness |
| 0023 | Offline complete Dispatch and unlisted capability refusal lack required observation | Move a compiled packet away from source repo; resolve all required context; plant unauthorized capability |
| 0027 | BLUT adapter typechecks but does not produce execution receipts | Consume real versioned BLUT receipts and refuse changed subject/lineage |
| 0028 | Client records do not establish KF server enforcement | Real typed action, direct-write refusal, stale concurrency and duplicate-action tests |
| 0029 | Registration Source Holder observation missing | Real registration plus before/after authored-atom identity and digest comparison |
| 0030 | Export implementation still names four required categories absent | Implement historical preservation/import; demonstrate superseded, disputed and annulled records survive |
| 0036 | autonomy::classify and lifecycle::needs_adr remain separate | Reconcile semantics, share classifier and exercise both callers |
| 0037 | Diff accepts baseline but lacks contracted --to input | Implement explicit comparison target and observe digest movement attributed to changed field |
| 0039 | Required detection case names unavailable commit3678455 | Recover history or prepare explicit historical reconciliation; synthetic substitute cannot satisfy named case |
| 0041 | Existing current plants need fresh bound observations | Capture telemetry at named revision; rerun empty-reviewer refusal and positive control |
| 0043 | Current controls include positive24 and shared30/32; old missing-control record is stale | Observe current controls and reconcile literal seven-negative-entry wording explicitly |
| 0044 | Real KF allocation, Git Source Holder and named scenarios remain missing | Check current provider availability and version, then run actual shared-contract scenarios; retain historical404 evidence |

Implementation pointers: `crates/openwarrant-core/src/preflight.rs`,
`execution.rs`, `seam.rs`, `autonomy.rs`, `lifecycle.rs`;
`crates/openwarrant-cli/src/blut.rs`, `kf.rs`, `export.rs`, `show.rs`;
`conformance/plants.d/00-corpus.sh`. Resolve each path from repository root.
Inspect the exact obligation before changing its implementation or evidence.

The other unsigned/incomplete scope records still need individual outcome review.
Notable remaining families: real model drafting (0035/0042), Katana execution
(0026/0045), BLUT execution (0047), Liminal and native-host parity (0040/0048),
schema consumption by KF (0032), old scope/authority corrections (0049), telemetry
and skills evidence (0063/0065/0066/0068), old CLI/UI delivery (0069/0070), and
old architecture/batch/TUI/adoption plans (0067/0071–0074). A modern successor
must name preserved scope and omitted obligations; changed product direction alone
never closes those original records.
