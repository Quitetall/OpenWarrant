obligation = "OBL-001"
disposition = "established"
evidence = "Test `adr::tests::supersession_relations_are_read_in_both_directions` (passing) directly exercises parsing of `supersedes` and `superseded_by` fields, and `lower::tests::relations_are_carried_through` (passing) confirms those relations survive the canonical round trip through the IR pipeline."
performer = "claude"

[verifier]
actor = "lamu:mimo-v2.5-pro"
kind = "agent"
model = "mimo-v2.5-pro"

[verifier.independence]
performer_transcript_blind = true
performer_rationale_blind = true
separate_writable_workspace = true
cannot_modify_subject_artifacts = true
cannot_modify_gate_definition = true
cannot_modify_gate_fixtures = true
separate_context_compilation = true
distinct_model_required = true
distinct_human_required = false
