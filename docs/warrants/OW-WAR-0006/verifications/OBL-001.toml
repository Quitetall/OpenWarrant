obligation = "OBL-001"
disposition = "not_established"
evidence = "The `AdrRecord` struct in `crates/openwarrant-core/src/adr.rs` has no fields for `supersedes` or `superseded_by` relations (only `status` with a `Superseded` variant and `governs` for warrants), and none of the six tests (`parses_an_adr_atom`, `missing_required_keys_fail_closed`, `unknown_status_is_refused`, `only_accepted_is_current`, `falsified_and_rejected_are_distinct`, `every_status_maps_to_an_appendix_a_section`) exercise parsing or round-tripping of supersession relations between ADRs."
performer = "claude"

[verifier]
actor = "lamu:mimo-v2.5-pro"
kind = "agent"
model = "mimo-v2.5-pro"

[verifier.independence]
performer_transcript_blind = true
performer_rationale_blind = true
separate_writable_workspace = true
cannot_modify_subject_artifacts = true
cannot_modify_gate_definition = true
cannot_modify_gate_fixtures = true
separate_context_compilation = true
distinct_model_required = true
distinct_human_required = false
