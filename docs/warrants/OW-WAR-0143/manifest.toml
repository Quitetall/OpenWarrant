# A Warrant is the contract for ONE bounded intervention inside a program,
# and it traces to that program's SAS through [[implements]] and [[roadmap]].
# Starting a program? Write its SAS instead (SAS §6.10; docs/DEFINITIONS.md).
schema = "oh.war/manifest/v1"
uuid = "01a0d289-1e92-7911-af69-ffdfe4fbbdbd"
local_alias = "OW-WAR-0143"

# Allocated only by Knowledge Fabric (SAS §12.4). Leave empty.
enterprise_id = ""

title = "war knows when it is old: build identity, a notice when a newer release exists, and an update that works from every version"
profile = "delivery"
assurance_level = "basic"

# No §106 row names installation, updating or build identity. The nearest
# normative text is §7 (readiness tests the tools actually used), Law 15 and
# §9; see atoms/20-basis.md.

[[roadmap]]
ref = "roadmap://OW-PHASE-9/self-update"

[[atoms]]
ordinal = 10
role = "intent"
path = "atoms/10-intent.md"
required = true

[[atoms]]
ordinal = 20
role = "basis"
path = "atoms/20-basis.md"
required = true

[[atoms]]
ordinal = 40
role = "work_order"
path = "atoms/40-work-order.md"
required = true

[[atoms]]
ordinal = 45
role = "milestones"
path = "atoms/45-milestones.yaml"
required = true

[[atoms]]
ordinal = 60
role = "assurance"
path = "atoms/60-assurance.md"
required = true

