obligation = "OBL-001"
disposition = "not_established"
evidence = "No test or measurement in the provided artifacts demonstrates that every state change goes through a typed and enveloped action; the tests in `kf::tests` (e.g., `a_short_idempotency_key_never_reaches_the_network`) only cover URL validation and idempotency key enforcement, not the typing and enveloping of all actions."
performer = "claude"

[verifier]
actor = "lamu:mimo-v2.5-pro"
kind = "agent"
model = "mimo-v2.5-pro"

[verifier.independence]
performer_transcript_blind = true
performer_rationale_blind = true
separate_writable_workspace = true
cannot_modify_subject_artifacts = true
cannot_modify_gate_definition = true
cannot_modify_gate_fixtures = true
separate_context_compilation = true
distinct_model_required = true
distinct_human_required = false
