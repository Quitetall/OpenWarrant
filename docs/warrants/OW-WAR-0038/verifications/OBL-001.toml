obligation = "OBL-001"
disposition = "established"
evidence = "migrate.rs::import records commit_sha in ImportArtifact and validates it is a full 40-char lowercase hex SHA; MigratedAdr.preserved_body stores the original body verbatim; MigratedAdr.validate recomputes the digest via an injected function and rejects mismatches; write_or_verify with verify_only=true compares a re-run against an existing artifact"
performer = "claude"

[verifier]
actor = "lamu:mimo-v2.5-pro"
kind = "agent"
model = "mimo-v2.5-pro"

[verifier.independence]
performer_transcript_blind = true
performer_rationale_blind = true
separate_writable_workspace = true
cannot_modify_subject_artifacts = true
cannot_modify_gate_definition = true
cannot_modify_gate_fixtures = true
separate_context_compilation = true
distinct_model_required = true
distinct_human_required = false
