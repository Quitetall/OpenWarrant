obligation = "OBL-002"
disposition = "not_established"
evidence = "crates/openwarrant-core/src/drafting.rs::ApplicationPipeline::may_apply - refuses when pre-write steps are incomplete, and tests verify the error is returned, but no test asserts the working tree or filesystem is unchanged after a refused proposal; the evidence requires that invariant to be asserted rather than assumed"
performer = "claude"

[verifier]
actor = "lamu:mimo-v2.5-pro"
kind = "agent"
model = "mimo-v2.5-pro"

[verifier.independence]
performer_transcript_blind = true
performer_rationale_blind = true
separate_writable_workspace = true
cannot_modify_subject_artifacts = true
cannot_modify_gate_definition = true
cannot_modify_gate_fixtures = true
separate_context_compilation = true
distinct_model_required = true
distinct_human_required = false
