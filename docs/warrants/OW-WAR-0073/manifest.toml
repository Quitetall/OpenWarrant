# A Warrant is the contract for ONE bounded intervention inside a program,
# and it traces to that program's SAS through [[implements]] and [[roadmap]].
# Starting a program? Write its SAS instead (SAS §6.10; docs/DEFINITIONS.md).
schema = "oh.war/manifest/v1"
uuid = "01a0983e-32db-7240-9a9d-cef5e5b41a38"
local_alias = "OW-WAR-0073"

# Allocated only by Knowledge Fabric (SAS §12.4). Leave empty.
enterprise_id = ""

title = "war tui: the dashboard in the terminal"
profile = "delivery"

# §21.2 / RQ-025. Superseded by OW-WAR-0112 on 2026-09-22, which adopts this
# work order's eight deliverables and four obligations verbatim and answers the
# T2 escalation below it: `war` with no subcommand opens the dashboard. This
# record stays available (§21.4). `currency` is a lifecycle state outside the
# contract digest (`ir.rs current_coverage`), so revision 1's signature stands.
currency = "superseded"
assurance_level = "basic"

# [[implements]]
# ref = "sas://WAR-SAS-RQ-000"
# contribution = "partial"

[[implements]]
ref = "sas://WAR-SAS-RQ-070"
contribution = "partial"

[[implements]]
ref = "sas://WAR-SAS-RQ-075"
contribution = "partial"

[[atoms]]
ordinal = 10
role = "intent"
path = "atoms/10-intent.md"
required = true

[[atoms]]
ordinal = 20
role = "basis"
path = "atoms/20-basis.md"
required = true

[[atoms]]
ordinal = 40
role = "work_order"
path = "atoms/40-work-order.md"
required = true

[[atoms]]
ordinal = 45
role = "milestones"
path = "atoms/45-milestones.yaml"
required = true

[[atoms]]
ordinal = 60
role = "assurance"
path = "atoms/60-assurance.md"
required = true

[[roadmap]]
ref = "roadmap://OW-PHASE-9/release"
