obligation = "OBL-001"
disposition = "not_established"
evidence = "The measured evidence includes a single receipt for 'GR-software.repo.war-check@1.0.0' and unit tests like 'canonical::tests::digests_are_stable_across_runs', but no CI run on both `x86_64-unknown-linux-gnu` and `aarch64-apple-darwin` with digest comparison is provided, so the cross-host determinism claim is not established."
performer = "claude"

[verifier]
actor = "lamu:mimo-v2.5-pro"
kind = "agent"
model = "mimo-v2.5-pro"

[verifier.independence]
performer_transcript_blind = true
performer_rationale_blind = true
separate_writable_workspace = true
cannot_modify_subject_artifacts = true
cannot_modify_gate_definition = true
cannot_modify_gate_fixtures = true
separate_context_compilation = true
distinct_model_required = true
distinct_human_required = false
