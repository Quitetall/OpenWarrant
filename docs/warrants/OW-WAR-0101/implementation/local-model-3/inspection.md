# Task/context layout observation

Source `c62fd2f`; same retained original request, exact model and CPU-only backend.
The local adapter exited zero after 88.38 seconds. Raw request, response, proposal,
model/server/adapter hashes and process limits are retained. No paid calls or
application occurred. The task-owned server was stopped after observation and its
process exited zero. Existing services were untouched.

Task fidelity failed. The model invented title `OW-WAR-0100` and scoped the result
to a changelog entry about that Warrant. The work order did not specify a project
changelog file or maintenance behavior. Assurance asserted properties of its own
draft instead of positive and refusal checks for the requested deliverable. These
errors violate the adapter prompt even though constrained fields and milestone
YAML conform. Four non-applying proposal checks passed; they do not prove fidelity.

Separating task text from request context did not establish this 4B model as a
useful autonomous drafter. Do not repair these bytes or treat syntactic success as
completion. Further work needs a capable explicitly configured backend or a
separate tested planning approach. No more model calls in this experiment.
