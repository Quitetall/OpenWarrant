# A Warrant is the contract for ONE bounded intervention inside a program,
# and it traces to that program's SAS through [[implements]] and [[roadmap]].
# Starting a program? Write its SAS instead (SAS §6.10; docs/DEFINITIONS.md).
schema = "oh.war/manifest/v1"
uuid = "01a0650e-5702-7f52-ba66-dbaee871efba"
local_alias = "OW-WAR-0063"

# Allocated only by Knowledge Fabric (SAS §12.4). Leave empty.
enterprise_id = ""

title = "Complete the conformance battery: the plants and tests the verifier named as missing"
profile = "delivery"
assurance_level = "basic"

# A Warrant implements a requirement that exists: `implements` refs are now
# held to §106 of the pinned SAS. Partial: RQ-022's traceability is OW-WAR-0013's.
[[implements]]
ref = "sas://WAR-SAS-RQ-022"
contribution = "partial"

# ADR supersession is a read relation in both directions (§19.3). Partial:
# RQ-020's federation is OW-WAR-0006's.
[[implements]]
ref = "sas://WAR-SAS-RQ-020"
contribution = "partial"

[[roadmap]]
ref = "roadmap://OW-PHASE-6/conformance"

[[atoms]]
ordinal = 10
role = "intent"
path = "atoms/10-intent.md"
required = true

[[atoms]]
ordinal = 20
role = "basis"
path = "atoms/20-basis.md"
required = true

[[atoms]]
ordinal = 40
role = "work_order"
path = "atoms/40-work-order.md"
required = true

[[atoms]]
ordinal = 45
role = "milestones"
path = "atoms/45-milestones.yaml"
required = true

[[atoms]]
ordinal = 60
role = "assurance"
path = "atoms/60-assurance.md"
required = true

